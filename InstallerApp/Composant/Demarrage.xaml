<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="InstallerApp.Composant.Demarrage">
    <VerticalStackLayout>
        <Label 
    Text="Installateur de base de Site Web Avancée"
    FontSize="Header"
    Style="{StaticResource Headline}"
    SemanticProperties.HeadingLevel="Level1"
    VerticalOptions="Center" 
    HorizontalOptions="Center" />

        <HorizontalStackLayout Spacing="80"
    VerticalOptions="Center"
    HorizontalOptions="Center">
            <Button Text="Installer" FontSize="Title" Clicked="Button_Install" />
            <Button Text="Quitter" FontSize="Title" Clicked="Button_Exit"/>
        </HorizontalStackLayout>
    </VerticalStackLayout>
</ContentView>
